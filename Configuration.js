const Configuration = {
    host: '127.0.0.1',
    port: 8080,
    app: 'Asimov',
    database: {
        host: 'localhost',
        port: 27017,
        database: 'asimov'
    }
}

module.exports = Configuration;